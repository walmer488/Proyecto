<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.insertarregistros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GuardarProducto">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InicioSistema">

        </activity>
        <activity android:name=".recycler_Categoria"></activity>
        <activity android:name=".GuardarCategoria" />
        <activity android:name=".recycler_Usuario" />
        <activity android:name=".GuardarUsuario" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>